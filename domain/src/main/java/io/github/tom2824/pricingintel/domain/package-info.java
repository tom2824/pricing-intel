/**
 * Modèle métier de Pricing Intel.
 *
 * <p>Règle absolue : ce paquet ne dépend de rien d'autre que le JDK. Pas de Spring, pas de Jackson,
 * pas de JPA. Tout ce qui est ici doit pouvoir être réutilisé tel quel dans un autre projet.
 * Cette règle est vérifiée par ArchUnit dans le module {@code architecture-tests}.
 */
package io.github.tom2824.pricingintel.domain;
