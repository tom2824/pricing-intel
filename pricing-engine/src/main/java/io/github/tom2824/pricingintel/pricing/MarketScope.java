package io.github.tom2824.pricingintel.pricing;

/** Périmètre d'un marché (ADR 0009) : le produit lui-même, ou les produits équivalents de son segment. */
public enum MarketScope {
    STRICT,
    SEGMENT
}
