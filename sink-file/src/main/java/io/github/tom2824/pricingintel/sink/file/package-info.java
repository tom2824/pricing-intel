/**
 * Sorties sur le système de fichiers : relevés en JSON Lines (une ligne JSON par relevé, facile à rejouer,
 * à diff-er et à charger n'importe où) et archivage compressé des pages brutes.
 * Ce sont les sorties par défaut quand on lance une collecte sans base de données.
 */
package io.github.tom2824.pricingintel.sink.file;
